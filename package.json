{
  "name": "cv",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@types/lodash": "^4.17.0",
    "lodash": "^4.18.1",
    "marked": "^15.0.0",
    "moment": "^2.30.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "astro": "^5.5.0",
    "pdf-lib": "^1.17.1",
    "puppeteer-core": "^24.39.1",
    "typescript": "^5.7.0"
  },
  "scripts": {
    "dev": "astro dev --port 3000",
    "start": "astro dev --port 3000",
    "build:site": "astro build",
    "generate": "npm run build:site && npm run generate:rtf && npm run generate:pdf",
    "generate:rtf": "node ./scripts/generate-rtf.mjs",
    "generate:pdf": "node ./scripts/generate-pdf.mjs",
    "build": "npm run build:site",
    "preview": "astro preview"
  }
}
